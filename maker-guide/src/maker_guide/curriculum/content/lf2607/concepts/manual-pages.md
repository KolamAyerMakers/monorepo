# Concept: Manual Pages

Skill: manual-pages

Manual pages are built-in documentation.

```bash
man ls
```

Press `q` to quit. Search with `/` when the pager supports it.

The manual is not cheating. It is the system telling you how it works.
