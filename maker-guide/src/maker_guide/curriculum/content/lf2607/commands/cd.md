# Command: `cd`

## Use

```bash
cd ~/src
```

## What It Does

`cd` changes the current working directory of your shell.

## Useful Forms

```bash
cd
cd ~/src
cd ..
cd -
```

- `cd` with no argument returns to your home directory.
- `cd ..` moves to the parent directory.
- `cd -` returns to the previous directory.

## Practice

Run `pwd` after `cd` until path changes are automatic in your head.

## Watch Out

`cd` affects the current shell. It does not move files.

## Docs Pointers

- Run `help cd`.
- Read [path](../concepts/path.md), [directory](../concepts/directory.md), and [filesystem](../concepts/filesystem.md).
