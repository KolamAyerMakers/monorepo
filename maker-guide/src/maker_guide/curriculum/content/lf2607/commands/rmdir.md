# Command: `rmdir`

Remove an empty directory.

```bash
mkdir ~/playground/empty-directory
rmdir ~/playground/empty-directory
```

If the directory contains files, `rmdir` refuses. That refusal is useful safety.
