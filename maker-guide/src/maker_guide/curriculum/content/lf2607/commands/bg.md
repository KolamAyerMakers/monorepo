# bg

## Use

```bash
bg
```

## What It Does

`bg` resumes a stopped job in the background.

## Practice

Stop a safe command with `Ctrl-Z`, then run `jobs`, then `bg`.

## Watch Out

Background jobs still belong to your shell. Use tmux for durable work.
