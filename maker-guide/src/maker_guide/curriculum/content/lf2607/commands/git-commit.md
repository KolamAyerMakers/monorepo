# git commit

## Use

```bash
git commit -m "describe the change"
```

## What It Does

`git commit` records staged changes in local history.

## Practice

Write messages that say why the change matters.

## Watch Out

If git says nothing is staged, run `git status` and stage the files you meant to save.

## Docs Pointers

- Read [git](git.md), [git status](git-status.md), and [git add](git-add.md).
- Run `git help commit`.
